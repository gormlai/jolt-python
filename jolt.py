import sys
from ctypes import *
import os

currentDirectory = os.getcwd()
buildDirectory = currentDirectory + "/build/"
print("current directory is " + currentDirectory)
#soFile = currentDirectory + 
